import 'package:flutter/cupertino.dart';

class ForTimeVideoOverlay extends StatelessWidget {
  const ForTimeVideoOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
