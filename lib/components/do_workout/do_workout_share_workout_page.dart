import 'package:flutter/cupertino.dart';

class DoWorkoutShareWorkoutPage extends StatelessWidget {
  const DoWorkoutShareWorkoutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
