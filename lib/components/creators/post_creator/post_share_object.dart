import 'package:flutter/cupertino.dart';

/// Create a getStream activity based on any shareable object and push it to user's feed.
/// /// No [page] suffix because this will usually be opened via a modal bottom sheet.
class PostShareObject extends StatelessWidget {
  const PostShareObject({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
