import 'package:flutter/cupertino.dart';

class BodyTransformationPage extends StatelessWidget {
  const BodyTransformationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
