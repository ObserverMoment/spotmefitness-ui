fragment UserFields on User {
  id
  avatarUri
  displayName
  hasOnboarded
  themeName
}

query authedUser {
  authedUser {
    ...UserFields
  }
}

mutation updateUser($data: UpdateUserInput!) {
  updateUser(data: $data) {
    ...UserFields
  }
}
