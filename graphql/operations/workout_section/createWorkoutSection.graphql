mutation createWorkoutSection(
  $data: CreateWorkoutSectionInput!
  $sort: Boolean
) {
  createWorkoutSection: createWorkoutSection(data: $data, sort: $sort) {
    ...WorkoutSection
    WorkoutSectionType {
      ...WorkoutSectionType
    }
  }
}
