mutation createWorkout($data: CreateWorkoutInput!) {
  createWorkout: createWorkout(data: $data) {
    ...WorkoutFields
    WorkoutGoals {
      ...WorkoutGoal
    }
    WorkoutTags {
      ...WorkoutTag
    }
  }
}
