# TODO: Needs to get back eaxctly the same data as workoutById
# Can you have nested fragments - if so make one called WorkoutData.
mutation updateWorkout($data: UpdateWorkoutInput!) {
  updateWorkout(data: $data) {
    ...Workout
  }
}
